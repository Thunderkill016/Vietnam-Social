# No Supabase credentials: read-only, explicitly labeled sample data.
# Copy the local API URL and anon/publishable key from `npx supabase status`.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
NEXT_PUBLIC_MAP_STYLE_URL=https://tiles.openfreemap.org/styles/positron
# Never add service_role / secret keys to NEXT_PUBLIC_* variables.
